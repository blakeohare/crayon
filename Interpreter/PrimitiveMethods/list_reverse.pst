if (argCount > 0) {
	return generateException(stack, pc, valueStackSize, ec, CoreExceptionTypes.INVALID_ARGUMENT, primitiveMethodWrongArgCountError("list reverse method", 0, argCount));
}
list1.Reverse();
