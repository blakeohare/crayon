Value lib_gamepad_function_poll_universe(Array<Value> args) {
    Value output = VALUE_NULL;
    if (@ext_boolean("LIB_GAMEPAD_SUPPORTED")) {
        Native._lib_gamepad_poll_universe();
    }
    return output;
}
