// $gamepad_clear_bindings(gamepad)
import inline "InterpreterSource/FrameworkFunctions/1_arg.cry";

bool1 = gamepadClearBindings(arg1);
if (!bool1) {
	lib_func_error_occurred = true;
	lib_func_error_message = "$gamepad_clear_bindings() requires a gamepad reference as its argument.";
}
