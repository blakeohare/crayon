/*
    args[0]: title as a (verified) non-empty string.
*/
VM.Value lib_game_set_title(VM.VmContext vm, Array<VM.Value> args) {
    Extension._lib_game_set_title((string) args[0].internalValue);
    return vm.globalNull;
}
