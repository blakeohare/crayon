/*
    args[0] -> font name

    returns boolean

    This value is cached outside of this function
*/
Value lib_graphics2dtext_isSystemFontPresent(VmContext vm, Array<Value> args) {
    return buildBoolean(vm.globals, Native._lib_graphics2dtext_isSystemFontAvailable((string)args[0].internalValue));
}
