/*
    args[0] -> volume ratio
*/
VM.Value lib_game_audio_music_set_volume(VM.VmContext vm, Array<VM.Value> args) {
    Extension._lib_game_audio_musicSetVolume((double) args[0].internalValue);
    return vm.globals.valueNull;
}
