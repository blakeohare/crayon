VM.Value lib_game_set_window_mode(VM.VmContext vm, Array<VM.Value> args) {
    int mode = (int) args[0].internalValue;
    int width = (int) args[1].internalValue;
    int height = (int) args[2].internalValue;

    Native._lib_game_set_window_mode(mode, width, height);

    return vm.globalNull;
}
