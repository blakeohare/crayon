Value lib_gamepad_is_supported(VmContext vm, Array<Value> args) {
    if (@ext_boolean("LIB_GAMEPAD_SUPPORTED") && Native._lib_gamepad_is_supported()) {
        return VALUE_TRUE;
    } else {
        return VALUE_FALSE;
    }
}
