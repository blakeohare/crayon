import FileIO;
import Extensions;
import Images;
import Resources;

class ExportDotNetExtensionService : ExtensionService {

	function handleRequest(args, cb) {
		exporter = new CSharpExporter(new CrayonExporter.BuildData(args));
		//try {
			exporter.export();
		//} catch (Exception e) {
		//	cb({ 'errors': [ e.message ]});
		//	return;
		//}
		
		cb({ 'errors': [] });
	}

	function handleRequestImpl(args) {
		
	}
}
