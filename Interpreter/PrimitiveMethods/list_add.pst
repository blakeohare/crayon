if (argCount == 0) {
	return Core.GenerateException(CoreExceptionTypes.INVALID_ARGUMENT, "List add method requires at least one argument.");
}
for (i = 0; i < argCount; i += 1) {
	list1.Add(funcArgs[i]);
}
output = VALUE_NULL;
