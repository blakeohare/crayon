Value lib_gamepad_platform_requires_refresh(VmContext vm, Array<Value> args) {
    if (@ext_boolean("LIB_GAMEPAD_SUPPORTED") && Native._lib_gamepad_platform_requires_refresh()) {
        return vm.globalTrue;
    }
    return vm.globalFalse;
}
