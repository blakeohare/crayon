// $sfx_load_from_resource(path)
import inline "InterpreterSource/FrameworkFunctions/1_arg.cry";

if (Value$arg1.type != Types.STRING) {
	lib_func_error_occurred = true;
	lib_func_error_message = "$sfx_load_from_resource requires a string argument.";
} else {
	output = loadSoundFromResource($_cast('string', Value$arg1.internalValue));
	if (value == null) {
		lib_func_error_occurred = true;
		lib_func_error_message = "Resource not found.";
	}
}
