namespace Nori {

	enum HorizontalAlignment {
		LEFT = 0,
		CENTER = 1,
		RIGHT = 2,
		STRETCH = 3,
	}
	
	enum VerticalAlignment {
		TOP = 0,
		CENTER = 1,
		BOTTOM = 2,
		STRETCH = 3,
	}

	enum _PositioningIndex {
		// These are acccessed by native code with hardcoded integers. Be sure to keep those in sync if these ever change.
		HORIZONTAL_ALIGNMENT = 0,
		LEFT_MARGIN = 1,
		RIGHT_MARGIN = 2,
		WIDTH = 3,
		VERTICAL_ALIGNMENT = 4,
		TOP_MARGIN = 5,
		BOTTOM_MARGIN = 6,
		HEIGHT = 7,
	}
}
