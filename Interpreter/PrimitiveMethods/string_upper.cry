
if (argCount > 0) {
	return $_exception(CoreExceptionTypes.INVALID_ARGUMENT, primitiveMethodWrongArgCountError("string upper method", 0, argCount));
}

output = buildString($_string_upper(string1));
