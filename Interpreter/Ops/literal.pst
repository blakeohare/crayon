@import("ValueStackCapacityCheck.cry");
if (Core.HasIncrement()) {
	valueStack[valueStackSize++] = literalTable[row[0]];
} else {
	valueStack[valueStackSize] = literalTable[row[0]];
	valueStackSize += 1;
}
