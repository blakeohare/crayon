function make_grid(width, height) {
    cols = [];
    for (x = 0; x < width; ++x) {
        col = [];
        for (y = 0; y < height; ++y) {
            col.add(null);
        }
        cols.add(col);
    }
    return cols;
}
