
if (argCount > 0) {
	return $_exception(CoreExceptionTypes.INVALID_ARGUMENT, primitiveMethodWrongArgCountError("string reverse method", 0, argCount));
}

output = buildString($_string_reverse(string1));
