
// Returns a status code
int _lib_gamegifcap_screenCap(Array<object> output);

// Saves a list of images to a file.
int _lib_gamegifcap_saveToDisk(string path, Array<object> images, int millisPerFrame);
