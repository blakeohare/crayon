/*
    returns 1 if:
    - this is JavaScript AND...
    - this is OSX

    otherwise, return 0

    Return null constant on platforms that don't support gamepads since this code doesn't get run anyway.
*/
Value lib_gamepad_jsIsOsx(VmContext vm, Array<Value> args) {
    if (@ext_boolean("LIB_GAMEPAD_SUPPORTED")) {
        return buildInteger(Native._lib_gamepad_jsIsOsx());
    } else {
        return VALUE_NULL;
    }
}
