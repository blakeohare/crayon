class AbstractScene {
	field next = this;
	
	constructor() { }
	
	function update(events, pressed_keys) {
		Core.assert(false, "Override me!");
	}
	
	function render(rc) {
		Core.assert(false,  "Override me!");
	}
}
