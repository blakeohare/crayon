// $gamepad_clear_ids()
bool1 = gamepadClearIds();
if (bool1) {
	lib_func_error_occurred = true;
	lib_func_error_message = "$gamepad_clear_ids() cannot be called at this time. Gamepad info is initialized when the game windows is launched.";
}
