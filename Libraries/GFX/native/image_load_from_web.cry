// $gfx_image_load_from_web(key, url)
import inline "FrameworkFunctions/2_arg.cry";

if (Value$arg1.type != Types.STRING || Value$arg2.type != Types.STRING) {
	lib_func_error_occurred = true;
	lib_func_error_message = "$image_load_from_web requires two string arguments.";
} else {
	string1 = $_cast('string', Value$arg2.internalValue);
	$_lib_gfx_download_image($_cast('string', Value$arg1.internalValue), string1);
}
