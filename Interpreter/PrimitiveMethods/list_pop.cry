
if (argCount > 0) {
	return $_exception(CoreExceptionTypes.INVALID_ARGUMENT, primitiveMethodWrongArgCountError("list pop method", 0, argCount));
}

len = $_list_length(list1);
if (len < 1) {
	return $_exception(CoreExceptionTypes.INDEX_OUT_OF_RANGE, "Cannot pop from an empty list.");
}

if (returnValueUsed) {
	output = $_list_get(list1, len - 1);
}

$_list_pop(list1);
