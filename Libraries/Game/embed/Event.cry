@localized('en', 'Game')
@localized('es', 'Juego')
namespace Game {

    // TODO: mark as abstract.
    // This can be extended in other libraries, such as Gamepad
    class Event {
        field type;
        constructor(type) {
            this.type = type;
        }
    }
}
