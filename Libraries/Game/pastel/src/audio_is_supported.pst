VM.Value lib_game_audio_is_supported(VM.VmContext vm, Array<VM.Value> args) {
    if (Extension._lib_game_audio_isAudioSupported()) {
        return vm.globals.boolTrue;
    }
    return vm.globals.boolFalse;
}
