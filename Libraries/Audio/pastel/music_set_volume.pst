/*
    args[0] -> volume ratio
*/
VM.Value lib_audio_music_set_volume(VM.VmContext vm, Array<VM.Value> args) {
    Native._lib_audio_musicSetVolume((double) args[0].internalValue);
    return vm.globals.valueNull;
}
