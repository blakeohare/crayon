using System;
using Interpreter;
using Interpreter.Vm;

namespace %%%PROJECT_ID%%%
{
    internal class Program
    {
        public static void Main(string[] args)
        {
            TranslationHelper.CommandLineArgs = args;
			TranslationHelper.LibraryRegistry = new LibraryRegistry();
            TranslationHelper.ByteCode = ResourceReader.ReadTextFile("ByteCode.txt");
            TranslationHelper.ResourceManifest = ResourceReader.ReadTextFile("ResourceManifest.txt");
            CrayonWrapper.v_main();
        }
    }
}
