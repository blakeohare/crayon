
if (argCount > 0) {
	return $_exception(CoreExceptionTypes.INVALID_ARGUMENT, primitiveMethodWrongArgCountError("string lower method", 0, argCount));
}

output = buildString($_string_lower(string1));
