
// row[0] -> member ID

// Assignments that override methods are blocked at compile-time.

value2 = stack.objectContext; // use of 'this' is blocked at compile time outside of non-static methods.

objInstance2 = ((ObjectInstance) value2.internalValue);
valueStackSize -= 1;
objInstance2.members[row[0]] = valueStack[valueStackSize];
