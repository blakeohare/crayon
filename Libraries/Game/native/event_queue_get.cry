/*
	arg 1: event instance
	arg 2: event queue index (predetermined to be safely in range)

*/

import inline "FrameworkFunctions/2_arg.cry";

objInstance1 = $_cast('ObjectInstance', Value$arg1.internalValue);
i = $_cast('int', Value$arg2.internalValue);

libGamePopulateEventNativeData(ObjectInstance$objInstance1.nativeData, i);
