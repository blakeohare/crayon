value = valueStack[(valueStackSize - 1)];
if (value.type != Types.BOOLEAN) {
	return Core.GenerateException(CoreExceptionTypes.INVALID_ARGUMENT, "Boolean expected.");
} else {
	if (((bool) value.internalValue)) {
		pc += row[0];
	} else {
		valueStackSize -= 1;
	}
}
