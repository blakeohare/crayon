Value lib_gamepad_refresh_devices(VmContext vm, Array<Value> args) {
    if (@ext_boolean("LIB_GAMEPAD_SUPPORTED")) {
        // No-op on most platforms.
        Native._lib_gamepad_refresh_devices();
    }
    return vm.globalNull;
}
