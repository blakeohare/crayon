/*
    arg[0]: Internal file path relative to where text files are stored. Not a user-defined path.
*/
Value lib_resources_readText(VmContext vm, Array<Value> args) {
    string string1 = Native._lib_resources_read_text((string) args[0].internalValue);
    return buildString(string1);
}
