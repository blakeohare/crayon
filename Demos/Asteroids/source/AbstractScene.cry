class AbstractScene {
	constructor() {
		this.next = this;
	}
	
	function update(events, pressed_keys) {
		$assert(false, "Override me");
	}
	
	function render(render_counter) {
		$assert(false, "Override me");
	}
}