// $gamepad_save_config()
int1 = gamepadSaveConfig();
if (int1 != 0) {
	if (int1 == 1)  return killExecution(stack, "$gamepad_save_config() cannot be called at this time. Gamepad info is initialized when the game windows is launched.");
}
