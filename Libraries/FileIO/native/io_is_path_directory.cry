// $io_is_path_directory(path)
import inline "FrameworkFunctions/1_arg.cry";

if (Value$arg1.type != Types.STRING) {
	lib_func_error_occurred = true;
	lib_func_error_message = "$io_is_path_directory requires a string argument.";
} else {
	bool1 = ioCheckPathExistence($_cast('string', Value$arg1.internalValue), false, false, true, stringOutParam);
	if (bool1) {
		output = VALUE_TRUE;
	} else {
		output = VALUE_FALSE;
	}
}
