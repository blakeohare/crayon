int ProcessHelper_processRun(
    ObjectInstance wrapper,
    string exPath,
    ListImpl args,
    Value onDataCb,
    string cwd) {

    if (cwd.Size() == 0) cwd = null;
    Array<int> intOut = new Array<int>(1);
    wrapper.nativeData = new Array<object>(1);
    wrapper.nativeData[0] = Extension.CoreFunc_ProcessRun(exPath, listImplToStringArray(args), onDataCb, intOut, cwd);

    return intOut[0];
}

void ProcessHelper_kill(ObjectInstance wrapper) {

}
