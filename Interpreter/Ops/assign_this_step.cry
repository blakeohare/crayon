
// row[0] -> member ID

// Assignments that override methods are blocked at compile-time.

value2 = StackFrame$stack.objectContext; // use of 'this' is blocked at compile time outside of non-static methods.

objInstance2 = $_cast('ObjectInstance', Value$value2.internalValue);
valueStackSize -= 1;
$_array_set(
	ObjectInstance$objInstance2.members,
	$_array_get(row, 0),
	$_array_get(valueStack, valueStackSize));
