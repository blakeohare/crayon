/*
    args[0] -> GLRenderer instance
        nativeData[0] -> POPULATES THIS FIELD: some native object that can take a character code and generate a bitmap that can be loaded as a GL texture
    args[1] -> sourceType
    args[2] -> sourceId
    args[3] -> size
    args[4] -> style

    No return type
    All fields already verified
    Populates the renderer in args[0].nativeData[0]

*/
Value lib_grahics2dtext_function_glGetBitmapTileRenderer(Array<Value> args) {
    return VALUE_NULL;
}
