VM.Value lib_audio_music_is_playing(VM.VmContext vm, Array<VM.Value> args) {
    if (Native._lib_audio_musicIsPlaying()) {
        return vm.globals.boolTrue;
    }
    return vm.globals.boolFalse;
}
