VM.Value lib_gamepad_poll_universe(VM.VmContext vm, Array<VM.Value> args) {
    if (@ext_boolean("LIB_GAMEPAD_SUPPORTED")) {
        Native._lib_gamepad_poll_universe();
    }
    return vm.globalNull;
}
