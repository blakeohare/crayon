
function gamepad_bind_analog(device, descriptor, alias) {
	return $$lib_gamepad_bind_analog(device, descriptor, alias);
}

function gamepad_bind_analog_1d(device, descriptor, alias, isPositive) {
	return $$lib_gamepad_bind_analog_1d(device, descriptor, alias, isPositive);
}

function gamepad_bind_analog_2d(device, descriptor, alias, isX, isPositive) {
	return $$lib_gamepad_bind_analog_2d(device, descriptor, alias, isX, isPositive);
}

function gamepad_bind_digital(device, descriptor, alias) {
	return $$lib_gamepad_bind_digital(device, descriptor, alias);
}

function gamepad_bind_digital_1d(device, descriptor, alias, isPositive) {
	return $$lib_gamepad_bind_digital_1d(device, descriptor, alias, isPositive);
}

function gamepad_bind_digital_2d(device, descriptor, alias, isX, isPositive) {
	return $$lib_gamepad_bind_digital_2d(device, descriptor, alias, isX, isPositive);
}

function gamepad_changed_recently(deviceOrPlayerId, alias) {
	return $$lib_gamepad_changed_recently(deviceOrPlayerId, alias);
}

function gamepad_clear_bindings(device) {
	return $$lib_gamepad_clear_bindings(device);
}

function gamepad_clear_ids() {
	return $$lib_gamepad_clear_ids();
}

function gamepad_count() {
	return $$lib_gamepad_count();
}

function gamepad_descriptor_guess(device, magic_string) {
	return $$lib_gamepad_descriptor_guess(device, magic_string);
}

function gamepad_enable_recent(playerIdOrIdList) {
	return $$lib_gamepad_enable_recent(playerIdOrIdList);
}

function gamepad_get_device(index) {
	return $$lib_gamepad_get_device(index);
}

function gamepad_get_state(deviceOrPlayerId, alias) {
	return $$lib_gamepad_get_state(deviceOrPlayerId, alias);
}

function gamepad_save_config() {
	return $$lib_gamepad_save_config();
}

function gamepad_set_id(device, id) {
	return $$lib_gamepad_set_id(device, id);
}
