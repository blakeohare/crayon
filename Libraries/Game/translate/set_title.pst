/*
    args[0]: title as a (verified) non-empty string.
*/
Value lib_game_function_set_title(Array<Value> args) {
    Native._lib_game_set_title(((string) args[0].internalValue));
    return VALUE_NULL;
}
