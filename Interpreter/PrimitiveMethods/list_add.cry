
if (argCount == 0) {
	return killExecution(stack, pc, "List add method requires at least one argument.");
}

for (i = 0; i < argCount; i += 1) {
	$_list_push(list1, $_array_get(funcArgs, i));
}
output := VALUE_NULL;
