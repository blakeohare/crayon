/*
	Creates a 2D grid of the given width and height.
	The X coordinate is the 1st dimension.
*/
function makeGrid(width, height) {
	columns = [];
	while (width-- > 0) {
		columns.add([null] * height);
	}
	return columns;
}
