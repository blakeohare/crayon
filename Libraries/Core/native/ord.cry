
// $ord(character)
import inline "FrameworkFunctions/1_arg.cry";

output = null;
if (Value$arg1.type == Types.STRING) {
	string1 = $_cast('string', Value$arg1.internalValue);
	if ($_string_length(string1) == 1) {
		output = buildInteger($_ord(string1));
	}
}
if (output == null) {
	lib_func_error_occurred = true;
	lib_func_error_message = "ord requires a 1 character string.";
}
