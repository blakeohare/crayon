if (argCount > 0) {
	return Core.GenerateException(CoreExceptionTypes.INVALID_ARGUMENT, primitiveMethodWrongArgCountError("list choice method", 0, argCount));
}
len = list1.Length;
if (len == 0) {
	return Core.GenerateException(CoreExceptionTypes.UNSUPPORTED_OPERATION, "Cannot use list.choice() method on an empty list.");
}
output = list1[Core.Int(((Core.RandomFloat() * len)))];
