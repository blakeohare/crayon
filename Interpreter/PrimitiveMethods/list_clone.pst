if (argCount > 0) {
	return Core.GenerateException(CoreExceptionTypes.INVALID_ARGUMENT, primitiveMethodWrongArgCountError("list clone method", 0, argCount));
}
list2 = new List<Value>();
len = list1.Length;
for (i = 0; i < len; i += 1) {
	list2.Add(list1[i]);
}
output = new Value(Types.LIST, list2);
