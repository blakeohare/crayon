/*
    args[0]: a re-usable value list to push event fields onto
*/
Value lib_game_function_pump_events(Array<Value> args) {
    libGamePumpEvents((List<Value>) args[0].internalValue);
    return args[0];
}
