VM.Value lib_game_audio_music_is_playing(VM.VmContext vm, Array<VM.Value> args) {
    if (Extension._lib_game_audio_musicIsPlaying()) {
        return vm.globals.boolTrue;
    }
    return vm.globals.boolFalse;
}
