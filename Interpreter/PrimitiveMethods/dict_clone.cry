
if (argCount > 0) {
	return $_exception(CoreExceptionTypes.INVALID_ARGUMENT, primitiveMethodWrongArgCountError("dictionary clone method", 0, argCount));
}

output = new Value(Types.DICTIONARY, cloneDictionary(dictImpl, null));
